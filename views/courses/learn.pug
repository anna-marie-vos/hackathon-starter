if learnCourses.length === 0 
    p You are not registered for any courses yet. 
each course in learnCourses
    .card.mr-1(style="width: 18rem;")
        .card-body.d-flex.flex-column
            h5.card-title #{course.title}
            p.flex-grow-1.card-text #{course.description}
            a.btn.btn-block.btn-outline-primary(href=`/courses/view/${course._id}`) View